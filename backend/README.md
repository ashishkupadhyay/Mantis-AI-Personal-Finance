# backend

FastAPI service for optional account features: Google sign-in, multi-device sync, statistical forecasting, classifier model registry, signed asset distribution and FCM push. Holds no LLM keys and proxies no prompts.

Placeholder until the backend milestone; nothing here is required to build or run the app.
