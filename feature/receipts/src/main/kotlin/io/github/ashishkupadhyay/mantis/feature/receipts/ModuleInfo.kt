package io.github.ashishkupadhyay.mantis.feature.receipts

/** Module marker for `:feature:receipts` (Scan bill and receipt review). Replaced by real code as work packages land. */
internal object ReceiptsModule {
    const val PATH: String = ":feature:receipts"
}
