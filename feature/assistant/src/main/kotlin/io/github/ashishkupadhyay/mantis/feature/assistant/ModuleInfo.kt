package io.github.ashishkupadhyay.mantis.feature.assistant

/** Module marker for `:feature:assistant` (Ask Mantis chat (BYOK provider)). Replaced by real code as work packages land. */
internal object AssistantModule {
    const val PATH: String = ":feature:assistant"
}
