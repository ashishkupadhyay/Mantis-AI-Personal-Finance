<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Backups are disabled entirely (NFR-20d): the database, key vault, models and receipts never leave the
        device through Auto Backup or device-to-device transfer. The in-app encrypted export (FR-PRV-1/2) is the
        only backup path. The extraction/backup rule files exclude everything as a second line of defence.
    -->
    <application
        android:name=".MantisApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mantis"
        tools:targetApi="s">

        <!-- singleTask: notification, widget and assistant deep links reuse the running task via onNewIntent. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Mantis"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- mantis:// links are allow-listed by DeepLinkParser (NFR-20f); unknown shapes open the app normally. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mantis" />
            </intent-filter>
            <!-- Shared CSV statements open the import wizard (FR-IMP-1); ShareIntentParser re-checks the type and URI. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
            </intent-filter>
        </activity>
    </application>

</manifest>
