package io.github.ashishkupadhyay.mantis.core.llm

/** Module marker for `:core:llm` (LlmProvider abstraction, BYOK adapters, KeyVault, UsageLedger, CapabilityProbe, JsonExtractor, ModelDownloader). Replaced by real code as work packages land. */
internal object LlmModule {
    const val PATH: String = ":core:llm"
}
