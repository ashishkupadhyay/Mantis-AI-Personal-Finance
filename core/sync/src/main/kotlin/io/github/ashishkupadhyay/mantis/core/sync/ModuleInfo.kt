package io.github.ashishkupadhyay.mantis.core.sync

/** Module marker for `:core:sync` (SyncEngine, SyncWorker, HLC, conflict application, FCM hint receiver). Replaced by real code as work packages land. */
internal object SyncModule {
    const val PATH: String = ":core:sync"
}
