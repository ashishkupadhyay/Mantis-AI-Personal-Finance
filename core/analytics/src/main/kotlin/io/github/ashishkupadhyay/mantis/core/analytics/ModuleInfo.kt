package io.github.ashishkupadhyay.mantis.core.analytics

/** Module marker for `:core:analytics` (Opt-in analytics facade (no-op by default)). Replaced by real code as work packages land. */
internal object AnalyticsModule {
    const val PATH: String = ":core:analytics"
}
