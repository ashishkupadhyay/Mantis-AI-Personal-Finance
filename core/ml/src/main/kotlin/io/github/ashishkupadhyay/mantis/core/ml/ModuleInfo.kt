package io.github.ashishkupadhyay.mantis.core.ml

/** Module marker for `:core:ml` (Normalizer, MerchantDictionary, RuleMatcher (RE2/J), LiteRT transaction + item classifiers, ModelManager). Replaced by real code as work packages land. */
internal object MlModule {
    const val PATH: String = ":core:ml"
}
