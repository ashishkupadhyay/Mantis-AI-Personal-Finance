package io.github.ashishkupadhyay.mantis.core.receipts

/** Module marker for `:core:receipts` (Document scanner wrapper, OCR, ReceiptParser, ItemCategorizer, ReceiptSelfCheck, ReceiptMatcher, ReceiptAiRepair). Replaced by real code as work packages land. */
internal object ReceiptsModule {
    const val PATH: String = ":core:receipts"
}
