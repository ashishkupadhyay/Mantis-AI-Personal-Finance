package io.github.ashishkupadhyay.mantis.core.network

/** Module marker for `:core:network` (Ktor client for the Mantis backend, DTOs, auth plugin). Replaced by real code as work packages land. */
internal object NetworkModule {
    const val PATH: String = ":core:network"
}
