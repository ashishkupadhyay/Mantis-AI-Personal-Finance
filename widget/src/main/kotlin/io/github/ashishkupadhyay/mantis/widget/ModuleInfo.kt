package io.github.ashishkupadhyay.mantis.widget

/** Module marker for `:widget` (Glance widgets (month at a glance, quick add)). Replaced by real code as work packages land. */
internal object WidgetModule {
    const val PATH: String = ":widget"
}
